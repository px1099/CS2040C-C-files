// Flying safely
#include <bits/stdc++.h>
using namespace std;

int main() {
	int TC;
	cin >> TC;
	for (int i=0; i<TC; i++) {
		int n, m;
		cin >> n >> m;
		for (int j=0; j<m; j++) {
			int a, b;
			cin >> a >> b;
		}
		cout << n-1 << endl;
	}
	return 0;
}